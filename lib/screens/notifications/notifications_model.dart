class NotificationModel {

  String? message;
  bool? isRead;
  DateTime? notificationTime;
  String? imageUrl;

  NotificationModel({this.message, this.isRead, this.notificationTime, this.imageUrl});
}